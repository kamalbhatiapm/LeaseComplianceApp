# Build output — generated by build.sh at deploy time; never commit locally-built files
# as they may contain injected env var values (SUPABASE_ANON_KEY etc.)
dist/

# Local environment overrides
.env
.env.local

# macOS
.DS_Store

# Editor
.vscode/
.idea/
